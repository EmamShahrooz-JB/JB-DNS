# بهبود پنل — 🎬 انیمیشن‌های Anime.js (ورکر v7)

- کتابخانهٔ **Anime.js v3.2.2** (MIT) به‌صورت inline داخل پنل embed شد —
  بدون CDN، خودکفا و مقاوم در شبکه‌های محدود (vendor/anime.min.js).
- انیمیشن‌های جدید:
  - **ورود:** ظهور پلکانی کارت لاگین + فنری قفل؛ **لرزش** کارت هنگام کلید غلط؛
    محو شدن لاگین هنگام ورود موفق + ظهور stagger شدهٔ سایدبار و هدر
  - **نمای کلی:** ورود stagger کارت‌های آمار + **شمارش انیمیت‌شدهٔ اعداد** (count-up) + ورود ردیف‌ها
  - **دراپ‌داون‌ها:** باز شدن فنری (spring) + ورود پلکانی آیتم‌ها (به جای CSS)
  - **تب‌ها:** ورود نرم هر نما (fade + slide)
  - **Toast:** بالا آمدن فنری از پایین صفحه
  - **مودال گفتگو:** باز شدن فنری + ظهور پلکانی حباب‌های پیام
  - **لوگو:** پالس پس از هر انتخاب موفق
- fallback کامل: اگر anime در دسترس نباشد همه‌چیز بدون انیمیشن ولی سالم کار می‌کند.
- تست‌ها: پنل sandbox ۶/۶ · ۱۵تایی کامل · دراپ‌داون ۱۹/۱۹ · تغییر کلید ۱۰/۱۰ — همه پاس.

---

# بهبود پنل — 🔑 تغییر کلید مدیریت + فونت وزیرمتن (ورکر v6)

- **فونت وزیرمتن** از CDN (jsdelivr — دسترس‌پذیر در ایران) در پنل بارگذاری می‌شود؛
  در صورت قطعیت، fallback به Tahoma.
- بخش جدید «🔑 تغییر کلید مدیریت» در تنظیمات پنل:
  - کلید فعلی + کلید جدید + تکرار؛ اعتبارسنجی ۸ تا ۶۴ نویسه
  - کلید جدید **به‌صورت هش SHA-256** در جدول `settings` ذخیره می‌شود (نه plaintext)
  - پس از تغییر، **فقط کلید جدید** معتبر است (کلید محیطی worker دیگر پذیرفته نمی‌شود)
  - سد تلاش: ۵ تلاش ناموفق در ساعت از هر IP
  - نشست فعلی خودکار به کلید جدید سوئیچ می‌شود
- endpoint جدید: `POST /admin/pass` با `{current, next}`
- جدول D1 جدید: `settings(key, value)`
- تست زندهٔ کامل چرخه روی سرور: تغییر → باطل‌شدن کلید قدیم (401) → کارکرد کلید جدید → بازگشت ✓
- ۱۰ تست DOM برای UI تغییر کلید + ۱۹ تست رگرسیون دراپ‌داون — همه پاس

---

# بهبود پنل — ✨ دراپ‌داون‌های سفارشی

منوهای انتخاب تنظیمات (قالب رنگی / حالت نمایش / زبان) از `select` بومی به
**دراپ‌داون سفارشی** ارتقا یافتند:

- انیمیشن فنری باز شدن (scale + overshoot) و ورود پلکانی آیتم‌ها (stagger)
- **نمونهٔ رنگی گرادیانی** برای هر تم + آیکن برای حالت نمایش و زبان
- تیک سبز روی گزینهٔ فعال؛ هایلایت هنگام hover؛ شِوِر چرخان روی تریگر
- پس‌زمینهٔ شیشه‌ای (backdrop blur) و سایهٔ عمیق
- فقط یک منو هم‌زمان باز می‌ماند؛ بستن با کلیک بیرون یا Escape
- بازخورد «اعمال شد ✓» با toast (دوزبانه)
- ۱۹ تست DOM برای رفتار کامل دراپ‌داون‌ها پاس شد

---

# تغییرات پنل مدیریت — 🎛️ طرح «دروازهٔ نهان» (ورکر v5)

بازطراحی کامل پنل `/admin` ورکر پشتیبانی به سبک داشبورد پروژهٔ
[Nahan Gateway](https://github.com/itsyebekhe/nahan) («دروازهٔ نهان»):

- صفحهٔ ورود با قفل تپنده، هاله‌های نورانی و کلید مدیریت (نشست ۳۰ دقیقه‌ای).
- سایدبار دسکتاپ + نویگیشن پایین موبایل؛ ۷ بخش: نمای کلی، گفتگوها،
  گزارش‌های قدیمی، سلامت سرویس، جریان پیام‌ها، تنظیمات، راهنما.
- کارت‌های آمار زنده: گفتگوها / پیام امروز / خوانده‌نشده / مجموع پیام‌ها / گزارش‌ها.
- **۵ قالب رنگی** (Indigo/Ocean/Forest/Sunset/Dracula) × روشن/تاریک — با ذخیرهٔ انتخاب.
- دوزبانهٔ کامل **فارسی/انگلیسی** با RTL/LTR خودکار.
- جستجو در گفتگوها؛ مودال گفتگو با پاسخ‌دهی و پیوست‌های بازشدنی (اطلاعات فنی/لاگ).
- endpoints جدید: `GET /stats` (آمار) و `GET /feed` (جریان پیام‌ها) — توکن‌محور.
- تازه‌سازی خودکار (آمار هر ۱۲ ثانیه، گفتگوی باز هر ۸ ثانیه) و toast اعلان‌ها.
- ۱۵ تست DOM روی پنلِ دیپلوی‌شده پاس شد (ورود غلط/درست، تب‌ها، آمار،
  مودال، i18n، تم‌ها، خروج).

---

# تغییرات نسخهٔ ۴٫۲ — 💬 گفتگوی پشتیبانی (چت تلگرام‌مانند)

بخش «گزارش مشکل» به یک **چت واقعی دوطرفه** ارتقا یافت: کاربر پیام می‌فرستد،
سازنده از پنل مدیریت پاسخ می‌دهد و پاسخ داخل خود اپ ظاهر می‌شود — مثل تلگرام.

## اپ (اندروید)
- ردیف «گفتگو با پشتیبانی» در تنظیمات → صفحهٔ چت تمام‌صفحه:
  حباب‌های پیام (کاربر راست/پشتیبانی چپ)، جداکنندهٔ روز («امروز»/«دیروز»)،
  ساعت فارسی هر پیام.
- **تیک‌های تلگرامی:** ⏱ در حال ارسال → ✓ رسید به سرور → ✓✓ آبی وقتی
  سازنده گفتگو را خوانده.
- دکمهٔ 📎 پیوست: «📊 اطلاعات فنی» (پیش‌فرض روشن)، «📜 ۲۰ لاگ اخیر»
  (پیش‌فرض خاموش) و «راه تماس» — انتخاب‌ها یادآوری می‌شوند.
- **پیام خودکار ربات** هنگام شروع گفتگو (خوش‌آمد + توضیح حریم خصوصی).
- شناسهٔ گفتگو (۱۶ hex) در localStorage — گفتگو پس از بازگشت ادامه می‌یابد؛
  همان شناسه «کد پیگیری» است.
- دریافت پیام‌های جدید هر ۱۵ ثانیه وقتی چت باز است (poll) + بلافاصله هنگام باز شدن.
- اعتبارسنجی دوطرفهٔ یکسان با سرور (متن ۵..۴۰۰۰، تماس ≤ ۲۰۰، لاگ ≤ ۳۰×۳۰۰).
- حالت نمایشی کامل بدون AND (پیش‌نمایش مرورگر).

## سرور (`cloudflare-worker/` — Worker + D1، نسخهٔ ۴)
- جدول‌های جدید `chats` + `messages`؛ `reports` برای سازگاری با APKهای 4.1 ماند.
- مسیرهای جدید: `POST /chat/send`، `GET /chat/poll`، و برای مدیر:
  `GET /chats`، `GET /chat?id=…`، `POST /chat/reply`.
- **پنل مدیریت جدید در `/admin`:** دو-ستونه شبیه تلگرام — فهرست گفتگوها با
  نشان «جدید»، خواندن گفتگو (✓✓ کاربر را فعال می‌کند)، پاسخ‌دادن، و
  تب «گزارش‌های قدیمی» برای فرم 4.1. تازه‌سازی خودکار هر ۸ ثانیه.
- محدودیت نرخ چت: ۳۰ پیام در ساعت از هر گفتگو؛ حذف خودکار +۹۰ روز.
- زنده: `https://jbdns-feedback.emam-shahrooz.workers.dev`

## تست
- **۸۴ تست واحد** (۹ تست Feedback برای payload چت) — همه پاس.
- **۱۹ تست DOM** برای UI چت: باز/بسته، حباب‌ها، تیک‌ها، XSS، پیوست‌ها،
  ماندگاری شناسه و تنظیمات.
- فیکس باگ تکرارشوندهٔ ردیف‌های تنظیمات: `chatRow` به استثناهای handler
  کلیک اضافه شد (مانند `rowBat` در 4.0).

---



بخش جدید در پایین تنظیمات: کاربر مشکلش را می‌نویسد و گزارش مستقیم به یک
**Cloudflare Worker** اختصاصی POST می‌شود.

## اپ (اندروید)
- فرم گزارش در پایین صفحهٔ تنظیمات: متن مشکل + راه تماس اختیاری.
- **حفظ حریم خصوصی با انتخاب صریح:**
  - «ارسال اطلاعات فنی» (پیش‌فرض روشن): نسخهٔ اپ و اندروید، مدل دستگاه،
    سرور فعال و پروتکل، وضعیت تونل، فهرست‌های فعال، آمار و آخرین خطا.
  - «ارسال ۲۰ لاگ اخیر» (پیش‌فرض **خاموش** — دامنه‌ها حساس‌اند).
- اعتبارسنجی دوطرفه یکسان (کلاینت + ورکر): متن ۵ تا ۴۰۰۰ نویسه، تماس تا ۲۰۰.
- بازخورد زنده: حالت «در حال ارسال…»، کد پیگیری پس از ثبت، پیام خطای فارسی؛
  مهلت ۳۰ ثانیه برای بی‌پاسخی سرور.
- ارسال از کاتلین با OkHttp (نه fetch مرورگر — دور از محدودیت CORS فایل‌های local).

## سرور (`cloudflare-worker/` — Cloudflare Worker + D1)
- ورکر با پایگاه‌دادهٔ **D1 (SQLite)**: جدول `reports` + `rate_limit`؛
  گزارش‌های قدیمی‌تر از ۹۰ روز هنگام هر ثبت خودکار حذف می‌شوند (تراکنش اتمیک).
- **محدودیت نرخ:** ۵ گزارش در ساعت از هر IP (IP فقط به‌صورت هش SHA-256).
- پنل مدیریت فارسی/RTL در `/admin` (توکن‌محور) + مسیرهای `list`/`one`.
- راهنمای دیپلوی با wrangler و بدون wrangler (داشبورد) در `cloudflare-worker/README.md`.

**فایل‌ها:** `core/Feedback.kt` (خالص و تست‌پذیر)، `MainActivity.kt` (پل
`sendFeedback` + ارسال OkHttp)، `index.html` (فرم + منطق UI)،
`cloudflare-worker/{worker.js,wrangler.toml,README.md}`، `FeedbackTest.kt`
(۶ تست). **مجموع ۸۱ تست پاس.** versionCode=15، versionName="4.1".

> ✅ ورکر با **D1** دیپلای و زنده است: `https://jbdns-feedback.emam-shahrooz.workers.dev`
> — ثبت `/report` ماندگار است (SQLite)، حذف خودکار ۹۰ روزه فعال، و
> آدرس واقعی در `Feedback.ENDPOINT` اپ ست شده است.

---

# تغییرات نسخهٔ ۴٫۰ — بزرگ‌ترین به‌روزرسانی: هر ۸ قابلیت در یک نسخه

> انتخاب کاربر: «همه بسته‌ها» — کاتالوگ، آمار، سلامت، مسابقه، بازنویسی،
> باتری، پشتیبان‌گیری و ویجت؛ با فهرست‌های داخل APK (بدون دانلود اولیه).

## ۱. 🗂 کاتالوگ فهرست‌های مسدودسازی آماده (الگوی RethinkDNS)
چهار فهرست فشردهٔ داخل برنامه — تنظیمات ← «فهرست‌های آمادهٔ مسدودسازی»:
- **تبلیغات** (۲٬۷۰۵ دامنه — Disconnect + شبکه‌های تبلیغاتی ایرانی مثل سباویژن)
- **ردیاب‌ها** (۱۳۸ — Disconnect + تحلیلگرها و فینگرپرینت‌ها)
- **بدافزار** (۴٬۰۴۲ — URLhaus، به‌روزِ روز)
- **خانواده** (۸۴ — سایت‌های بزرگسال + TLDهای xxx/porn/adult)
پیش‌فرض: سه فهرست اول روشن. مجموع ~۷ هزار دامنه، افزایش APK فقط ~۰٫۱MB.
همراه با «فهرست راه دور» قبلی (hosts) و دامنه‌های دلخواه کاربر فعال می‌شوند.

## ۲. 📊 نمودار و پربارش‌ترین‌ها (الگوی NetGuard)
صفحهٔ لاگ‌ها حالا با یک نگاه: نمودار ۲۴ ساعتهٔ پرس‌وجوها (میله‌های قرمز =
مسدودشده) + پربارش‌ترین دامنه‌های امروز با نوار نسبت — و لمس هر دامنه همان
کنترل مسدود/استثنای لاگ را باز می‌کند.

## ۳. 🩺 تست سلامت داخلی (الگوی Intra)
دکمهٔ «سلامت» در صفحهٔ خانه؛ شش بررسی واقعی با پرس‌وجوی زنده:
تونل فعال؟ رمزنگاری سرتاسر؟ سرور پاسخ می‌دهد (با تأخیر واقعی)؟ پاسخ
دامنهٔ تست **IP فیلترینگ (10.10.34.x) ندارد**؟ مسدودسازی کار می‌کند؟
Private DNS اندروید در حالت عادی است؟

## ۴. ⚡ حالت مسابقه — Race (الگوی lb_strategy در dnscrypt-proxy)
سوییچ «حالت مسابقه» در تنظیمات ← اتصال: پرس‌وجو هم‌زمان از سرور فعال و
همهٔ پشتیبان‌ها می‌رود و **سریع‌ترین پاسخ برنده است** — سرعت دیگر تابع
کندترین عضو زنجیره نیست. برچسب اعلان به سرورِ برندهٔ هر مسابقه به‌روز می‌شود.

## ۵. ✏️ بازنویسی DNS و بلاک وایلدکارد (الگوی cloaking در dnscrypt-proxy)
- قواعد «دامنه → IP ثابت»: پاسخ فوری و **آفلاین** (بدون رفتن به سرور) —
  برای دستگاه‌های خانگی و توسعه. AAAA هم به‌درستی پاسخ خالی می‌گیرد.
- بلاک زیردامنه‌ها: نوشتن `example.com` در دامنه‌های مسدود، خودش
  `a.example.com` و `x.y.example.com` را هم می‌گیرد (سندش در تست‌ها).

## ۶. 🔋 ویزارد باتری/اجرای خودکار (دردِدل مشترک Intra/RethinkDNS)
تنظیمات ← «جلوگیری از بسته‌شدن»: تشخیص سازنده (شیائومی/هواوی/OPPO/vivo/…)
و گام‌های دقیق همان گوشی + دکمهٔ «باز کردن تنظیمات» که مستقیم صفحهٔ
Autostart/Power Keeper/بهینه‌سازی باتری را باز می‌کند (با fallback عمومی).

## ۷. 💾 پشتیبان‌گیری و انتقال تنظیمات (الگوی RethinkDNS)
خروجی JSON همهٔ تنظیمات (سرورها + سفارشی‌ها + زنجیره + بلاک‌لیست‌ها +
بازنویسی‌ها + سوییچ‌ها) با پنجرهٔ اشتراک اندروید؛ بازیابی با انتخاب فایل —
اعتبارسنجی نسخه و رد فایل‌های غیر-JB-DNS.

## ۸. 📲 ویجت صفحهٔ اصلی
دکمهٔ وصل/قطع ۱×۱ روی هوم‌اسکرین — همان منطق کاشوی سریع (رضایت داده شده
یعنی اتصال بدون باز شدن برنامه). وضعیت ویجت با هر تغییر تونل تازه می‌شود.

## 🐛 رفع باگ‌ها
- **زنجیرهٔ پشتیبان در اتصال تازه اعمال نمی‌شد** (باقی‌مانده از v3.5):
  موقع اولین اتصال فقط ریزالور تکی ساخته می‌شد و زنجیره پس از اولین تغییر
  تنظیمات سر جایش می‌نشست — حالا از همان startTunnel با buildResolver ساخته می‌شود.
- allowList هم در اتصال تازه اعمال می‌شود (مانند بالا).
- پاک‌سازی باقی‌ماندهٔ اندک از فایل‌های قبلی.

## 🔧 جزئیات فنی
- `BlocklistCatalog.kt` (کش درون‌فرایندی)، `RaceResolver` (استخر اختصاصی،
  نخستین پاسخ موفق با CAS برنده می‌شود)، `HealthCheck.kt` (خالص و تست‌پذیر)،
  `BatteryWizard.kt` (گام‌ها + intent های سازنده‌ها)، `SettingsBackup.kt`
  (مدل خالص + JSON)، `TunnelWidget.kt` + layout/drawable/xml.
- `DnsStub`: پارامتر `rewriteMap` — بازنویسی قبل از فهرست مسدودسازی
  (قاعدهٔ صریح کاربر برنده است) و بدون رفتن به بالادست.
- `VpnStats`: سطل‌های ساعتی + شمارندهٔ دامنه‌ها (سقف حافظه با بریدن به
  پرتکرارترین‌ها).
- پل‌های JS جدید: getCatalog/setCatalog، getRewrites/setRewrite،
  runHealthCheck، stats24، getBatterySteps/openBatteryStep،
  exportSettings/pickBackupFile، `__health`/`__imported` push.
- تست واحد: `org.json:json` واقعی به testImplementation (نسخهٔ اندروید stub است).
- versionCode=14، versionName="4.0". **مجموع ۷۵ تست پاس** (۵۸ + ۱۷ جدید:
  Race 4، HealthCheck 4، SettingsBackup 4، Rewrite/Wildcard 5).

---

# تغییرات نسخهٔ ۳٫۶ — بازطراحی کامل شیت جزئیات سرور

## 🎨 شیت سرور: از فرم ساده به کارت شیشه‌ای مدرن
شیت پایینی که با لمس هر سرور باز می‌شود، از پایه بازطراحی شد — همان
قابلیت‌ها، ولی با ظاهری که «حال می‌کند»:

- **هویت بصری:** آواتار گرادیانی با حرف اول نام سرور؛ رنگ آواتار از دستهٔ
  سرور می‌آید (ایرانی=فیروزه‌ای، سراسری=آبی، حریم خصوصی=بنفش،
  مسدودساز=سبز، سفارشی=نارنجی). زیرش چیپ‌های وضعیت: «فقط داخل ایران»،
  «در زنجیرهٔ پشتیبان»، «سرور فعال».
- **کارت نشانی با کپی یک‌ضربه‌ای** — به‌جای متن خام؛ برای DNSCrypt مهر
  `sdns://` کپی می‌شود (با fallback به `execCommand` برای WebView).
- **پروتکل به‌صورت سگمنت‌کنترل** با آیکون (قفل DoH، سپر DoT، کلید
  DNSCrypt، کرهٔ DNS) به‌جای چهار دکمهٔ خاکستریِ هم‌شکل.
- **سنجش و پشتیبان کنار هم** در دو کارت اکشن — به‌جای سه دکمهٔ پهن روی هم.
- **نمایش تأخیر با نوار رنگی:** سبز < ۱۲۰ms، کهربایی < ۲۶۰ms، قرمز بیشتر؛
  هنگام سنجش نشانگر چشمک‌زن + نوار متحرک؛ خطاها (مثل «فقط از داخل
  ایران») با نقطهٔ قرمز.
- **دکمهٔ اصلی گرادیانی «فعال‌سازی این سرور»** — و اگر همین سرور فعال
  است، حالت خنثی «این سرور فعال است».
- **بدنهٔ شیشه‌ای همهٔ شیت‌ها:** بلور ۲۶px + اشباع رنگ + هالهٔ نور بالای
  شیت + انیمیشن ورود پلکانی آیتم‌ها (شیت لاگ، افزودن DNS و اپ‌ها هم از
  همان بدنه بهره می‌برند).
- پاک‌سازی ۵۳۱ بایت آشغال انتهایی فایل (باقی‌ماندهٔ بی‌ضرر از پچ‌های
  قبلی).

**فایل‌ها:** فقط `index.html` (CSS + HTML + JS شیت) و `build.gradle.kts`
(versionCode=13). تغییری در لایهٔ بومی نیست — همان ۵۸ تست پاس شد.
فایل `preview-server-sheet.html` برای دیدن تعاملی طراحی داخل مرورگر ساخته
شد (خودکار شیت را باز می‌کند).

---

# تغییرات نسخهٔ ۳٫۵ — بستهٔ پایداری: زنجیرهٔ پشتیبان + کاشوی سریع + کنترل از لاگ

## ۱. 🔗 زنجیرهٔ سرورهای پشتیبان (Failover)
دیگر قطعی یک سرور یعنی قطع اینترنت نیست:
- در برگهٔ هر سرور، دکمهٔ «＋ افزودن به زنجیرهٔ پشتیبان»؛ سرورهای پشتیبان با
  چیپ سبز «پشتیبان» در فهرست مشخص‌اند.
- اگر سرور فعال قطع شود (مثلاً فیلترینگ موقت)، پرس‌وجوی بعدی خودکار از سرور
  پشتیبان پاسخ می‌گیرد — بدون دست‌زدن کاربر. برچسب اعلان و وضعیت هم‌زمان
  به‌روز می‌شود.
- **کاوش بازیابی:** هر ۳۰ ثانیه سرور اولویت‌بالای زنجیره دوباره آزموده
  می‌شود؛ بازیابی که شد، خودکار برمی‌گردد (کشفِ بازیابی ارزان است چون
  کلید قطع‌کنِ سرورِ مرده فوری خطا می‌دهد).
- هم‌افزایی با کلید قطع‌کن نسخهٔ ۳٫۴: هر حلقهٔ زنجیره خودش fail-fast است.

## ۲. 📲 کاشوی تنظیمات سریع — بدون باز کردن برنامه
کاشوی JB-DNS (که از نسخهٔ ۱ در نوار تنظیمات سریع بود) حالا کامل شد:
- **وصل با یک ضربه و بدون باز شدن برنامه** (اگر رضایت VPN قبلاً داده شده).
- قطع همچنان مستقیم.
- وضعیت کاشو (روشن/خاموش + نام سرور فعال) به‌محض وصل/قطع تازه می‌شود
  (`requestListeningState`).

## ۳. 🎯 کنترل مسدودسازی از صفحهٔ لاگ‌ها
روی هر پرس‌وجو در لاگ‌ها بزنید:
- **«🚫 برای همیشه مسدودش کن»** — دامنه به دامنه‌های مسدود دلخواه اضافه و
  بی‌درنگ روی تونلِ روشن اعمال می‌شود.
- **«✅ هرگز مسدود نشود (استثنا)»** — فهرست استثنای جدید: دامنه‌هایی که حتی
  اگر در فهرست‌های تبلیغات/بدافزار باشند پاسخ می‌گیرند (برای وقتی یک سایت
  با بلاک‌لیست به‌هم می‌ریزد).

**فایل‌ها:** `UpstreamResolvers.kt` (ChainResolver با کاوش بازیابی)،
`DnsVpnService.kt` (ساخت زنجیره + allowList + رفرش کاشو)، `DnsStub.kt`
(allowList)، `Prefs.kt` (backupIds/allowedDomains)، `TunnelTile.kt` (شروع
مستقیم)، `MainActivity.kt` (پل‌های getBackups/setBackup/addBlockedDomain/
addAllowDomain)، `index.html` (دکمه/چیپ پشتیبان + شیت لاگ)،
`FailoverAndAllowTest.kt` (۴ تست جدید). **مجموع ۵۸ تست پاس.**

---

# تغییرات نسخهٔ ۳.۴ — رفع نشت DNS، سرورهای مرده و DoH کند

## تحقیق از پروژه‌های موفق (خواستهٔ کاربر)
مطابق درخواست، هر رفع با الگوی پروژه‌های اثبات‌شده مقایسه شد:
- **Intra (Jigsaw):** مسیر کامل 0.0.0.0/0 + تشخیص حالت strict اندروید (Private DNS)
  و bootstrap با IP — الگوی مسیرها و هشدار از همین‌جا آمد.
- **NetGuard:** الگوی رهگیری DNSهای هاردکدشده (ریدایرکت پورت ۵۳).
- **dnscrypt-proxy:** رفتار timeout کوتاه و fail-fast (الگوی کلید قطع‌کن).

## ۱. 🛡️ رفع نشت DNS
سه مسیر نشت بسته بود و بسته شد:
- **DNSهای هاردکدشدهٔ برنامه‌ها:** بعضی اپ‌ها مستقیم به 8.8.8.8 و 1.1.1.1
  پرس‌وجو می‌فرستند و از تونل رد می‌شدند. حالا مسیرِ ۱۶ DNS عمومی معروف
  (Google/Cloudflare/Quad9/OpenDNS/Yandex/AdGuard/CleanBrowsing) به تونل
  افزوده می‌شود (الگوی NetGuard) و استاب ما پاسخ می‌دهد.
- **DNSهای واقعی شبکه:** DNSهای LinkProperties شبکهٔ فعال هم مسیردهی
  می‌شوند تا هیچ پرس‌وجویی از شبکهٔ بی‌رمز نگذرد.
- **Private DNS اندروید:** در حالت strict، سیستم از DoT خودش می‌رود و تونل
  را دور می‌زند. حالا برنامه این را تشخیص می‌دهد و در بخش «اتصال» بنر قرمز
  راهنما نشان می‌دهد (الگوی Intra).

## ۲. ⚡ سرورهای مرده دیگر پرس‌وجوها را معطل نمی‌کنند
- **کلید قطع‌کن (circuit breaker):** بعد از ۳ شکست پیاپی، سرور اصلی ۳۰ ثانیه
  دور زده می‌شود و پرس‌وجوها بی‌درنگ پاسخ جایگزین می‌گیرند — نه اینکه هر کدام
  منتظر کامل timeout بمانند. پس از ۳۰ ثانیه دوباره سرور اصلی آزمایش می‌شود.
- **timeout ها از ۸ به ۵ ثانیه:** سرورِ بلاک‌شدهٔ DPI باید سریع شکست بخورد.
- **DoH با اولویت IPv4:** روی شبکه‌های با IPv6 ناقص، تلاش اول فقط روی IPv4
  (رفع تأخیر اتصال اول).
- ۳ تست جدید برای کلید قطع‌کن (`CircuitBreakerTest`) — مجموع ۵۴ تست پاس.

## ۳. 🔒 «بازگشت به DNS ساده» پیش‌فرضش خاموش شد
این تنظیم در ایران خطرناک بود: وقتی DoH/DoT توسط DPI بلاک می‌شود، fallback
روی پورت ۵۳ یعنی پاسخِ های‌جک‌شدهٔ ISP و عملاً «نشت + تحریم برمی‌گردد» —
کاربر فکر می‌کند وصل است ولی DNS واقعی عوض نشده. حالا پیش‌فرض خاموش است
(حاضرین می‌توانند از تنظیمات روشن کنند).

## ۴. 🔗 DNSCrypt در همه‌جا قابل انتخاب
دکمهٔ DNSCrypt در برگهٔ هر سرور و در شیت «افزودن DNS سفارشی» اضافه شد؛
سرور دلخواه با نشانی `sdns://AQ…` (از dnscrypt.info) قابل افزودن است.

**فایل‌ها:** `DnsVpnService.kt` (مسیرهای ضد-نشت)، `UpstreamResolvers.kt`
(circuit breaker + DoH IPv4-first)، `UpstreamFactory.kt` (timeout های ۵s)،
`MainActivity.kt` (تشخیص Private DNS + sup در serverJson)، `Prefs.kt`
(fallback پیش‌فرض خاموش)، `index.html` (DNSCrypt UI + بنر Private DNS)،
`CircuitBreakerTest.kt` (جدید)، `build.gradle.kts` (versionCode=11/3.4).

---

# تغییرات نسخهٔ ۳.۳ — سرعت، تعویض سرور زنده و آیکون برنامه‌ها

## ۱. ⚡ سه برابر شدن سرعت پاسخ‌دهی DNS (رفع کندی لود عکس و محتوا)
ریشهٔ اصلی کندی پیدا شد: پرس‌وجوهای DoT و DNSCrypt با `@Synchronized`
روی «یک اتصال» سریال می‌شدند — یعنی هر دامنهٔ جدید باید منتظر رفت‌وبرگشتِ
کامل دامنهٔ قبلی می‌ماند. یک صفحهٔ وب با ۱۰ دامنه = ۱۰ RTT پشت هم!
- **DoT:** بازنویسی با «استخر اتصال» — تا ۴ اتصال TLS ماندگار به‌طور موازی
  استفاده می‌شوند؛ اتصال آزاد از استخر برمی‌گردد و در خطا با اتصال تازه یک‌بار
  تلاش مجدد می‌شود (رفتار قبلی حفظ شد).
- **DNSCrypt:** قفل `resolve` حذف شد (گواهی همچنان قفل دارد؛ سوکت‌ها
  per-query‌اند) — پرس‌وجوها کاملاً موازی.
- DoH از قبل با OkHttp (اتصال ماندگار + HTTP/2 موازی) درست بود؛ DNS ساده
  هم per-query است.
- نتیجه: مرورگر که ده‌ها lookup را هم‌زمان می‌فرستد، دیگر صف نمی‌شود؛
  لود تصاویر و محتوا به‌محض اولین پاسخ شروع می‌شود.

## ۲. 📴 اولویت IPv4 به‌صورت پیش‌فرض (مشکل «گوگل و کلادفلر»)
Google و Cloudflare بزرگ‌ترین ناشران IPv6 (AAAA) هستند؛ روی شبکه‌های ایرانی
که IPv6 آن‌ها ناقص/خراب است، مرورگر اول IPv6 را امتحان می‌کند و تا timeout
می‌ماند — همین «اصلاً خوب کار نمی‌کنند» است. حالا «مسدودسازی IPv6»
**پیش‌فرض روشن** است (پاسخ خالی AAAA → فوری fallback به IPv4). اگر شبکهٔ
شما IPv6 سالم دارد، همان سوییچ قبلی در تنظیمات خاموشش می‌کند.

## ۳. 🔄 تعویض سرور بدون ری‌استارت (رفع باگ)
`setServer` فقط تنظیم را ذخیره می‌کرد و ریزالورِ تونلِ در حال اجرا را عوض
نمی‌کرد — سرور واقعی فقط بعد از بستن و باز کردن برنامه عوض می‌شد. حالا
بلافاصله `applySettings` صدا زده می‌شود: ریزالور بالادست، برچسب‌های اعلان و
حافظهٔ نهان همان لحظه تازه می‌شوند.

## ۴. 🖼️ آیکون واقعی برنامه‌ها در تونل افتراقی
فهرست برنامه‌های Split Tunneling حالا آیکون واقعی هر اپ را نشان می‌دهد
(به‌جای حرف اول رنگی):
- پل `getAppIcon(pkg)` آیکون را به WebP فشردهٔ ۹۶px تبدیل و base64
  می‌کند؛ با حافظهٔ نهان.
- بارگذاری تنبل با `IntersectionObserver` — فقط آیکون‌های قابل‌مشاهده
  واکشی می‌شوند تا فهرست‌های بزرگ سریع بمانند.
- اگر آیکون در دسترس نبود، آواتار حرف‌رنگی قبلی نشان داده می‌شود.

**فایل‌ها:** `UpstreamResolvers.kt` (DotResolver با استخر اتصال)،
`Dnscrypt.kt` (resolve موازی)، `MainActivity.kt` (setServer + getAppIcon)،
`Prefs.kt` (blockIpv6 پیش‌فرض true)، `index.html` (آیکون تنبل + ipv6)،
`build.gradle.kts` (versionCode=10/versionName=3.3). ۵۱ تست پاس — از جمله
تست‌های زندهٔ DoT/DNSCrypt با معماری جدید.

---

# تغییرات نسخهٔ ۳.۲ — اعلام صوتی اتصال

## ۱. 🔊 ویس «DNS Online / DNS Offline»
صدای وضعیت اتصال با ویس‌های اختصاصی کاربر اعلام می‌شود:
- **DNS Online:** بلافاصله بعد از برقراری موفق تونل (شامل شروع خودکار پس از بوت).
- **DNS Offline:** هنگام قطع شدن — با دکمهٔ قطع، اعلان، `onRevoke` یا شکست
  بازسازی تونل.
- صداها **فقط در تغییر واقعی وضعیت** پخش می‌شوند نه در بازسازی‌های داخلی
  (watchdog/restart) تا تکرار آزاردهنده نباشد.
- فایل‌های دریافتی (MP3) با ffmpeg به **OGG Vorbis مونو ۴۴.۱kHz** تبدیل و
  سکوت ابتدا/انتها حذف شد (Online: ۱.۳ ثانیه، Offline: ۱.۶ ثانیه).
- پخش با `MediaPlayer` + آزادسازی خودکار پس از پایان؛ خطاها بی‌صدا مدیریت
  می‌شوند.

## ۲. 🎚️ سوییچ «اعلان صوتی اتصال»
در تنظیمات ← اتصال؛ پیش‌فرض **روشن**. با خاموش کردن آن هیچ صدایی پخش
نمی‌شود (کلید `sound_notify`).

**فایل‌ها:** `res/raw/dns_online.ogg` و `res/raw/dns_offline.ogg` (جدید)،
`DnsVpnService.kt` (تابع announce + هوک‌های startTunnel/stopVpn)، `Prefs.kt`
(soundNotify)، `MainActivity.kt` (پل sound)، `index.html` (سوییچ)،
`build.gradle.kts` (versionCode=9/versionName=3.2). ۵۱ تست پاس.

---

# تغییرات نسخهٔ ۳.۱ — تونل افتراقی جدید: سوییچ + عبور از تونل

## ۱. 🎚️ سوییچ روشن/خاموش برای تونل افتراقی
بخش «تونل» در تنظیمات حالا یک سوییچ uiverse (همان طرح سایر سوییچ‌های برنامه)
دارد:
- **خاموش (پیش‌فرض):** ردیف انتخاب برنامه‌ها **خاکستری و غیرقابل کلیک** است
  (`opacity + grayscale + pointer-events:none`) و کل تونل DNS مثل قبل برای
  همهٔ برنامه‌ها فعال می‌ماند.
- **روشن:** ردیف به حالت عادی برمی‌گردد و می‌توان برنامه‌ها را انتخاب کرد.

## ۲. 🔀 تغییر معنا: انتخاب‌شده‌ها «عبور از تونل» می‌کنند
سمانتیک از «مستثنا کردن» به **«فقط همین برنامه‌ها از تونل VPN عبور کنند»**
تغییر کرد:
- در `DnsVpnService.setupTunnel` وقتی سوییچ روشن است و حداقل یک برنامه انتخاب
  شده، از `Builder.addAllowedApplication` استفاده می‌شود — یعنی DNS فقط
  برنامه‌های انتخاب‌شده از تونل رمزنگاری‌شده می‌گذرد و DNS بقیهٔ برنامه‌ها
  مستقیم از شبکهٔ اصلی پاسخ می‌گیرد.
- خودِ برنامه هرگز در فهرست مجاز نیست (جلوگیری از حلقهٔ DNS بالادست) و چون
  allowed/disallowed در اندروید با هم مجاز نیستند، استثنای خودِ برنامه فقط در
  حالت عادی با `addDisallowedApplication` اعمال می‌شود.
- فهرست خالی + سوییچ روشن = رفتار عادی همه-از-تونل (fallback امن).
- تغییر سوییچ یا فهرست، تونلِ در حال اجرا را بی‌درنگ با `restartTunnel`
  بازسازی می‌کند (allowed apps فقط با establish جدید اعمال می‌شود).

## ۳. 🐛 رفع یک باگ قدیمی: تنظیمات ذخیره نمی‌شد
کلیک روی سوییچ‌های تنظیمات (مسدودسازی تبلیغات، Sinkhole، IPv6، گواهی TLS،
بازگشت به DNS ساده، شروع خودکار) فقط وضعیت را در حافظهٔ WebView عوض می‌کرد و
`AND.setSetting` هرگز صدا زده نمی‌شد — یعنی نه ذخیره می‌شد و نه روی تونل اعمال.
حالا همهٔ سوییچ‌ها از همان مسیر `setSetting` اندروید ذخیره و اعمال می‌شوند.

**فایل‌ها:** `Prefs.kt` (splitTunnelingEnabled + tunneledPackages با کلیدهای
جدید؛ کلید قدیمی excluded_apps کنار گذاشته شد)، `DnsVpnService.kt` (منطق
include-mode)، `MainActivity.kt` (getTunneled/setTunneled/setSetting split/
getSettings.split)، `index.html` (سوییچ + حالت خاکستری + متن‌ها)،
`build.gradle.kts` (versionCode=8/versionName=3.1). ۵۱ تست پاس.

---

# تغییرات نسخهٔ ۳.۰ — DNSCrypt + دامنه‌های مسدود دلخواه

## ۱. 🔐 DNSCrypt v2 — پیاده‌سازی کامل پروتکل
چهارمین پروتکل رمزنگاری‌شده اضافه شد؛ همهٔ لایه‌ها دست‌ساز و تست‌شده:
- **تجزیهٔ نشانی DNS Stamp** (`sdns://AQ…`) — طرح رسمی DNS Stamps.
- **واکشی و تأیید گواهی سرور:** پرس‌وجوی TXT برای نام ارائه‌دهنده (با بازکردن
  رشته‌های طول‌دار TXT)، انتخاب گواهی دارای بیشترین serial در بازهٔ اعتبار،
  **تأیید امضای Ed25519** با کلید عمومی ارائه‌دهنده از stamp.
- **رمزنگاری پرس‌وجو/پاسخ:** X25519 برای توافق کلید + هر دو ساختار گواهی:
  XChaCha20-Poly1305 (es=2) و XSalsa20-Poly1305 (es=1) با پیاده‌سازی مستقل
  HSalsa20/HChaCha20/ChaCha20-block و BouncyCastle برای باقی اجزا.
- **فرمت دقیق wire (مطابق dnscrypt-proxy):** فقط نیمهٔ اول نانس (۱۲ بایت) روی
  شبکه ارسال می‌شود؛ کل بستهٔ نهایی حداقل ۵۱۲ بایت و ضریب ۶۴ (سربار ثابت
  ۶۸ = magic+pk+نصف‌نانس+MAC)؛ MAC جلو (فرمت secretbox).
- **es=2 به سبک xsecretbox:** کلید Poly1305 = ۳۲ بایت اول بلوک صفرِ XChaCha20،
  ۳۲ بایت اول پیام با ادامهٔ همان بلوک رمز می‌شود (ساختار NaCl secretbox،
  نه AEAD-IETF) — دقیقاً مانند پیاده‌سازی مرجع dnscrypt-proxy.
- **دو سرور داخلی جدید:** «AdGuard DNSCrypt» (es=1/XSalsa) و «Quad9 DNSCrypt»
  (es=2/XChaCha) — stampهای زنده از فهرست رسمی dnscrypt-resolvers.
- افزودن هر سرور DNSCrypt دلخواه با sdns:// در شیت «افزودن DNS سفارشی».
- **۱۳ تست در `DnscryptTest`:** دو تست زندهٔ واقعی به AdGuard و Quad9 +
  وکتورهای رسمی HChaCha20 (draft-irtf-cfrg-xchacha)، X25519 (RFC 7748)،
  secretbox و xsecretbox (تولیدشده با libsodium) + تست‌های stamp و رفت‌وبرگشت.

## ۱.۱. 🧪 یادداشت مهندسی — صحت‌سنجی با مرجع
در طول توسعه، هر لایه با مرجع مستقل صحت‌سنجی شد: وکتورهای RFC 7748/8439 و
draft XChaCha برای بلوک‌های رمزنگاری؛ dnscrypt-proxy 2.1.5 واقعی روی سندباکس
راه انداخته شد و پکت‌های wire آن با tcpdump ضبط و بایت‌به‌بایت با پکت‌های ما
مقایسه شد؛ دو انحراف ظریف پروتکل (ارسال کامل نانس به‌جای نصف آن، و حداقل
اندازهٔ ۲۵۶ به‌جای ۵۱۲) و یک انحراف رمزنگاری (استفاده از AEAD-IETF به‌جای
ساختار NaCl xsecretbox برای es=2) همین‌طور پیدا و رفع شدند — نتیجه: پاسخ
رمزگشایی‌شدهٔ سرور در تست زنده.

## ۲. 🚫 دامنه‌های مسدود دلخواه — UI جدید
`extraBlockedDomains` از قبل در هسته بود ولی رابط کاربری نداشت؛ حالا در تنظیمات ←
«مسدودسازی پیشرفته» ← «دامنه‌های مسدود دلخواه»: ویرایشگر چندخطی (هر خط یک دامنه)،
ذخیرهٔ فوری و اعمال بی‌درنگ روی تونلِ روشن.

## 🔍 چرا DoQ و فایروال کامل نه (فعلاً)؟
- **DoQ (DNS-over-QUIC)** به یک پشتهٔ QUIC کامل نیاز دارد (Cronet حدود ۱۰MB به
  حجم اپ می‌افزاید و وابستگی به Play Services می‌سازد). DoH و DoT و حالا DNSCrypt
  همگی رمزنگاری‌شده‌اند و پوشش شبکه‌ای کامل می‌دهند.
- **فایروال per-app کامل** (مثل RethinkDNS) نیازمند مسیر catch-all و بازتولید کل
  معماری «تونل فقط-DNS» است — تصمیم بزرگی است که با اصل «سرعت اینترنت شما تغییر
  نمی‌کند» این برنامه در تضاد است. (اسپلیت تانلینگ سبک ما همین حالا هم بدون این
  هزینه کار می‌کند.)

**فایل‌ها:** `net/Dnscrypt.kt` (جدید، ~۵۰۰ خط)، `DnscryptTest.kt` (جدید، ۱۳ تست)،
`Server.kt`، `Proto` (در Server.kt)، `BuiltInServers.kt`، `UpstreamFactory.kt`،
`MainActivity.kt`، `index.html`، `build.gradle.kts` (BouncyCastle،
versionCode=7/versionName=3.0).

---

# تغییرات نسخهٔ ۲.۵ — الگوهای طلایی از پروژه‌های موفق دنیا

نتیجهٔ تحقیق روی کد منبع Intra (Jigsaw)، DNS66، RethinkDNS و personalDNSfilter؛
شش قابلیت کلیدی آن‌ها که JB-DNS نداشت، اضافه شد:

## ۱. 🌐 مدیریت تغییر شبکه (الگوی NetworkManager در Intra)
قبلاً با تعویض WiFi↔دادهٔ همراه، سوکت‌های بالادست به شبکهٔ مرده گره می‌خوردند و
DNS تا ری‌استارت دستی از کار می‌افتاد. حالا با `registerDefaultNetworkCallback`:
- هر شبکهٔ جدید → `setUnderlyingNetworks` (گره‌خوردن سوکت‌ها به شبکهٔ فعال) + خالی‌کردن
  حافظهٔ نهان DNS (پاسخ‌های شبکهٔ قبلی دیگر معتبر نیستند)
- قطع شبکه → `setUnderlyingNetworks(null)`

## ۲. 🐕 نگهبان زنده‌مانی تونل (الگوی VpnWatchdog در DNS66)
هر ۳۰ ثانیه زنده‌بودن نخِ تونل بررسی می‌شود؛ اگر به دلایل غیرمنتظره مرده باشد،
تونل خودکار بازسازی می‌شود — با خنک‌کردن ۳۰ ثانیه‌ای تا حلقهٔ ری‌استارت شکل نگیرد.

## ۳. 🚫 فهرست مسدودسازی راه دور با قالب hosts (الگوی RethinkDNS و personalDNSfilter)
«Pi-hole شخصی»: نشانی هر فایل hosts (StevenBlack، AdAway، …) را در تنظیمات بدهید؛
دانلود (سقف ۱۶MB/۱M دامنه)، تجزیهٔ قالب‌های hosts کلاسیک / دامنهٔ ساده / سینتکس
AdGuard، ذخیره در حافظهٔ دستگاه و اعمال بی‌درنگ روی تونل روشن. حذف نشانی = پاک‌سازی.

## ۴. 🔔 آمار زنده روی اعلان پایدار (الگوی Intra)
اعلان پایدار هر ۳۰ ثانیه شمار پرس‌وجوها و مسدودشده‌ها را به‌روز می‌کند
(فقط هنگام تغییر اعداد — بدون بار اضافی).

## ۵. 🔄 شروع خودکار بعد از به‌روزرسانی برنامه (الگوی AutoStarter در Intra)
اکشن `MY_PACKAGE_REPLACED` به BootReceiver اضافه شد — آپدیت برنامه دیگر
اتصال کاربر را نمی‌کشد (اگر «شروع خودکار» روشن باشد).

## ۶. 🧹 بهبودهای کوچک
- `applySettings` پس از موفقیت دانلود فهرست، بلاک‌لیست را بدون قطع اتصال عوض می‌کند.

**فایل‌ها:** `DnsVpnService.kt` (callback شبکه + نگهبان + آمار اعلان)،
`RemoteBlocklist.kt` (جدید)، `Prefs.kt`، `MainActivity.kt` (پل getBlocklist/setBlocklistUrl)،
`BootReceiver.kt`، `AndroidManifest.xml`، `strings.xml`، `index.html`،
`versionCode=6/versionName=2.5`.

---

# تغییرات نسخهٔ ۲.۴ — رفع «DNSهای بی‌پاسخ»، پاک‌سازی متن‌های ناخوانا و ترمیم اسپلیت تانلینگ

## 🔴 چرا «بعضی DNS ها کار نمی‌کردند»؟ (تست واقعی همهٔ سرورها انجام شد)
- **سرور پیش‌فرض Shecan بود** — Shecan و همهٔ سرورهای ایرانی فقط از داخل شبکهٔ
  ایران پاسخ می‌دهند؛ بیرون از ایران (مثلاً اروپا) هیچ‌وقت جواب نمی‌دهند و
  کاربر فکر می‌کرد «DNS کار نمی‌کند».
  **اصلاح:** در اولین اجرا یک پروب واقعی (UDP به Shecan با مهلت ۱٫۵ ثانیه) انجام
  می‌شود؛ اگر Shecan مرده ولی Cloudflare زنده بود، پیش‌فرض خودکار به Cloudflare
  تغییر می‌کند (داخل ایران همان Shecan می‌ماند).
- **برچسب «داخل ایران»** برای هر ۱۳ سرور ایرانی در فهرست سرورها + توضیح در برگهٔ
  جزئیات + پیام خطای سنجش: «فقط از داخل ایران پاسخ می‌دهد».
- **DNS.WATCH حذف شد** — این سرویس عملاً از کار افتاده است.
- آدرس‌های DoH در برگهٔ سرور به‌جای URL کامل، فقط نام میزبان نمایش داده می‌شود.

## 🔴 متن‌های «کدمانند» پایین صفحه حذف شدند
- **پنل پیش‌نمایش دسکتاپ (`.side`)** با لینک‌های دانلود `JB-DNS-1.0.apk ⬇`، برچسب‌های
  انگلیسی و متن فنی — به‌کلی حذف شد؛ رابط حالا در همهٔ اندازه‌ها تمام‌صفحه و
  اپ‌مانند است.
- مهار overscroll (`overscroll-behavior:none`) — کشیدن بیش‌ازحد صفحه دیگر چیزی
  از پشت لایه‌ها بیرون نمی‌کشد.
- فارسی‌سازی باقی‌مانده‌ها: «(Split Tunneling)» ← «(تونل افتراقی)»، خط اعتبار،
  شمارهٔ نسخهٔ صفحهٔ درباره.

## 🔴 سوییچ‌های تنظیمات — کد جدید (طراحی uiverse)
سوییچ نئومورفیک بازگردانده شد — دقیقاً با پارامترهای کد ارسالی شما:
`--accent-hue:22deg` (اکسنت گرم)، `--easing:cubic-bezier(0,0,0,1)`،
`--duration:.6s`، `transition:all` و لایهٔ `::after` روی نشانگر چپ — با مقیاس‌بندی
سایه‌ها برای عرض ۵۸px و رنگ‌های سازگار با تم روشن/تیره.

## 🔴 اسپلیت تانلینگ — علت اصلی خرابی پیدا و ترمیم شد
در بازبینی سورس معلوم شد بخشی از متدهای پل (`getPackages/getExcluded/setExcluded`)
و `restartTunnel` به‌واسطهٔ یک تداخل ویرایشی از فایل‌ها حذف شده بودند — یعنی
فهرست برنامه‌ها همیشه خالی بود! ترمیم‌ها:
- هر سه متد پل بازافزوده شدند + مسیر جایگزین `getInstalledPackages` برای رام‌هایی
  که فهرست لانچر را خالی برمی‌گردانند.
- `restartTunnel` با debounce 400ms روی نخ پس‌زمینه (نه نخ اصلی) + `@Volatile` برای fd.
- بازخورد فوری: با هر تیک، پیام «از تونل DNS خارج شد · در حال اعمال…».
- اگر فهرست خالی بود، دکمهٔ «تلاش دوباره» نمایش داده می‌شود.

## 🟠 سایر ترمیم‌های همزمان
- دکمهٔ «قطع اتصال» اعلان با `PendingIntent.getService` مستقیم به خود سرویس می‌رود.
- fd تونل مسدودکننده (blocking) شد — الگوی DNS66، مصرف CPU صفر در بیکاری.
- گفت‌وگوی مجوز VPN و تغییر پوسته همگی روی نخ اصلی اجرا می‌شوند.

**فایل‌ها:** `index.html`، `MainActivity.kt`، `DnsVpnService.kt`، `Server.kt`
(iranOnly)، `BuiltInServers.kt` (حذف DNS.WATCH)، `AndroidManifest.xml`،
`versionCode=5/versionName=2.4`.

---

# تغییرات نسخهٔ ۲.۳ — رفع قطع‌نشدن دکمه و زنده‌ماندن تونل پس از بستن برنامه

## 🔴 ریشهٔ «اتصال الکی» و مرگ تونل با بسته‌شدن برنامه
سرویس foreground با نوع `connectedDevice` در **اندروید ۱۴+** به یکی از مجوزهای
`CHANGE_NETWORK_STATE` / `BLUETOOTH_CONNECT` / … نیاز دارد؛ ما آن را نداشتیم.
نتیجه: `startForeground(..., CONNECTED_DEVICE)` با SecurityException می‌مُرد →
تونل واقعی برقرار نمی‌ماند و همراه برنامه از بین می‌رفت.
**اصلاح:** مجوز `CHANGE_NETWORK_STATE` به مانیفست اضافه شد — دقیقاً همان
ترکیبی که Intra (با همین نوع سرویس) استفاده می‌کند.

## 🔴 ریشهٔ «کارنکردن دکمهٔ قطع»
- **قطع اتصال حالا مستقیم و قطعی است:** `disconnect()` از پل، ابتدا
  `DnsVpnService.instance?.stopVpn()` را صدا می‌زند (بستن fd تونل، توقف نخ‌ها،
  حذف اعلان، stopSelf) و `stopService` فقط پشتیبان است. قبلاً همه‌چیز به یک
  `stopService` واگذار شده بود.
- **دکمهٔ «قطع اتصال» روی اعلان** حالا با `PendingIntent.getService` و اکشن
  `ACTION_STOP` مستقیماً به خود سرویس می‌رود (بدون واسطهٔ broadcast که روی
  بعضی دستگاه‌ها ناکارامد بود).
- **مقاوم‌سازی JS:** `stop()` با try/catch، تلاش دوباره پس از ۱٫۸ ثانیه و
  آزادسازی اضطراری دکمه پس از ۳٫۸ ثانیه — دکمه دیگر هرگز قفل نمی‌شود.
- **گفت‌وگوی مجوز VPN از نخ اصلی:** `vpnPermission.launch()` روی نخ JavaBridge
  وب‌ویو صدا زده می‌شد که می‌توانست گفت‌وگو را خراب کند؛ حالا با runOnUiThread
  اجرا می‌شود (رفع یک منشأ احتمالی قفل‌شدن دکمه در اولین اتصال).
- `setTheme` هم (به‌دلیل بازسازی اکتیویتی) به نخ اصلی منتقل شد.

## 🟢 زنده‌ماندن DNS پس از بستن برنامه
- با رفع مشکل مجوز اندروید ۱۴، سرویس یک foreground service واقعی است و از
  بسته‌شدن اکتیویتی جداست.
- `android:stopWithTask="false"` روی سرویس + override صریح `onTaskRemoved`
  (بدون توقف) — کشیدن برنامه از Recentها تونل را قطع نمی‌کند.
- `START_STICKY` + BootReceiver: پس از کشته‌شدن فرایند یا ری‌استارت گوشی
  (با شروع خودکار) تونل دوباره بالا می‌آید.
- باز کردن دوبارهٔ برنامه با تونلِ روشن: وضعیت بلافاصله همگام می‌شود
  (push فوری پس از بارگذاری + sync در ۰٫۴ ثانیهٔ نخست).

## 🟠 پایداری تونل
- **حلقهٔ خواندن تونل به fd مسدودکننده (blocking) تغییر کرد** — الگوی DNS66:
  نخ در بیکاری صفر CPU مصرف می‌کند و با بستن fd در زمان توقف بیدار می‌شود.
  (حذف حلقهٔ poll با مکث ۱۰ms.)
- `onDestroy` سرویس حالا fd تونل را می‌بندد و ارجاع نخ را پاک می‌کند.

**فایل‌های تغییر یافته:** `AndroidManifest.xml`، `DnsVpnService.kt`،
`MainActivity.kt`، `index.html`، `build.gradle.kts` (versionCode=4/2.3).

---

# تغییرات نسخهٔ ۲.۲ — دو قابلیت جدید

## ۱. سوییچ‌های تنظیمات به سبک نسخهٔ بومی (Material 3)
سوییچ‌های صفحهٔ تنظیمات در WebView از طرح نئومورفیک قبلی به **سوییچ استاندارد
Material 3** (همان `MaterialSwitch` نسخهٔ بومی ۱.۰) تغییر کرد:

- مسیر ۵۲×۳۲ با گوشهٔ کامل؛ حالت خاموش: پس‌زمینهٔ سطح + حاشیهٔ ۲px (outline)؛
  حالت روشن: پر با رنگ primary پالت اقیانوسی.
- thumb: خاموش = ۱۶px (رنگ outline) ← روشن = ۲۴px (سفید/ocean_900) با
  **حرکت فنری با overshoot** (`cubic-bezier(.3,1.3,.4,1)`) — دقیقاً حس M3.
- هنگام فشردن ردیف، thumb مثل M3 بزرگ می‌شود (۲۶/۲۸px).
- جهت حرکت thumb با RTL هماهنگ است (آغاز = راست).

## ۲. تونل افتراقی (Split Tunneling) — مثل Intra
انتخاب برنامه‌هایی که DNSشان باید از تونل خارج بماند:

- صفحهٔ تنظیمات ← گروه «تونل» ← «اپلیکیشن‌های مستثنا».
- شیت پایینی با جست‌وجوی زنده، آواتار حرف اول هر برنامه و چک‌باکس انیمیشنی.
- ذخیرهٔ پایدار در `SharedPreferences` (`excluded_apps`).
- اعمال روی تونل: `addDisallowedApplication` برای هر بستهٔ انتخابی در
  `setupTunnel()` — و اگر تونل روشن باشد، **بازسازی بی‌درز تونل** با debounce
  ۴۰۰ms (الگوی `restartVpn` در Intra وقتی APPS_KEY عوض می‌شود).
- فهرست برنامه‌ها از طریق `<queries>` مانیفست (MAIN/LAUNCHER) به‌دست می‌آید؛
  یعنی **بدون** مجوز حساس `QUERY_ALL_PACKAGES`.
- در پیش‌نمایش مرورگر (بدون پل اندروید) فهرست نمونه نمایش داده می‌شود.

**فایل‌های تغییر یافته:** `index.html` (CSS سوییچ M3 + کارت و شیت تونل افتراقی)،
`Prefs.kt`، `DnsVpnService.kt` (اعمال مستثناها + `restartTunnel`)،
`MainActivity.kt` (پل `getPackages/getExcluded/setExcluded`)،
`AndroidManifest.xml` (`<queries>`)، `versionCode=3/versionName=2.2`.

---

# تغییرات نسخهٔ ۲.۱ — بازبینی بر پایهٔ کد Intra (Jigsaw/Google)


این نسخه حاصل مقایسهٔ خط‌به‌خط JB-DNS با پروژهٔ مرجع
[Intra](https://github.com/Jigsaw-Code/Intra) (و در موارد معماری تونل فقط-DNS، با
[DNS66](https://github.com/julian-klode/dns66)) است. همهٔ اشتباهات پیدا شده اصلاح شدند.

---

## 🔴 باگ‌های بحرانی (احتمال کرش یا کارنکردن کامل)

### ۱. مسیر تونل برای DNS جعلی اضافه نشده بود — مهم‌ترین باگ
`VpnService.Builder.addDnsServer()` به‌تنهایی هیچ مسیری در جدول مسیریابی VPN
نمی‌سازد؛ در نتیجه بسته‌های DNS مقصدِ `10.210.0.2` ممکن بود اصلاً وارد تونل نشوند و
برنامه روی بسیاری از دستگاه‌ها به‌کلی کار نمی‌کرد.
**اصلاح:** `addRoute(LOCAL_DNS_IP, 32)` — دقیقاً همان کاری که DNS66
(`addRoute(alias, 32)`) و NetGuard انجام می‌دهند. همچنان هیچ مسیر catch-all وجود
ندارد، پس فقط DNS وارد تونل می‌شود.

### ۲. کرش «did not then call Service.startForeground»
اگر `establish()` شکست بخورد (مثلاً VPN دیگری فعال باشد)، سرویس بدون فراخوانی
`startForeground` متوقف می‌شد → روی اندروید ۸+ کرش اجباری.
**اصلاح (الگوی Intra):** `startForeground` اکنون نخستین کار `onStartCommand` است،
پیش از هر کاری که ممکن است شکست بخورد.

### ۳. کرش `MissingForegroundServiceTypeException` در اندروید ۱۴+
با targetSdk ۳۴، سرویس foreground باید «نوع» داشته باشد؛ این نوع نه در مانیفست بود
و نه در `startForeground` پاس داده می‌شد.
**اصلاح (عیناً الگوی Intra):** `android:foregroundServiceType="connectedDevice"` در
مانیفست + مجوز `FOREGROUND_SERVICE_CONNECTED_DEVICE` + فراخوانی سه‌آرگومانی
`startForeground` در اندروید ۱۳+.

### ۴. تأیید گواهی DoT عملاً بی‌اثر بود (باگ امنیتی)
پیاده‌سازی قبلی `session.peerHost` را با نام میزبان مقایسه می‌کرد؛ برای سوکتی که با
`createSocket()` ساخته و بعداً `connect()` شده بود، این مقدار نتیجهٔ reverse-DNS یا
تهی است — یعنی گواهی سرور هرگز واقعاً بررسی نمی‌شد و SNI هم ارسال نمی‌شد.
**اصلاح:** لایه‌گذاری TLS با `createSocket(raw, hostname, port, true)` (نام میزبان
در دسترس JSSE → SNI درست) + تأیید استاندارد نام میزبان با
`SSLParameters.endpointIdentificationAlgorithm = "HTTPS"`.
تست زندهٔ DoT روی Google/Quad9/Cloudflare/AdGuard اعتبارسنجی‌اش می‌کند.

---

## 🟠 باگ‌های عملکردی

### ۵. حلقهٔ خواندن تونل CPU را می‌سوزاند (busy-spin)
تونل non-blocking است ولی وقتی داده‌ای نبود، حلقه با `continue` بی‌وقفه می‌چرخید.
**اصلاح:** مکث ۱۰ms هنگام خالی‌بودن تونل (الگوی درست: Intra از epoll در tun2socks
استفاده می‌کند؛ در جاوا معادلِ بی‌بار-CPU همین poll با تأخیر کوتاه است).

### ۶. همهٔ پرس‌وجوهای DNS پشت هم صف می‌شدند
`DnsStub.handle` کل متد `@Synchronized` بود و چون فراخوانی سرور بالادست داخل همان
قفل است، یک سرورِ کند (تایم‌اوت ۸ ثانیه) کل DNS دستگاه را می‌خواباند — با وجود
۸ نخ کارگر! فلسفهٔ Intra (`Race.java`) موازی‌بودن است.
**اصلاح:** قفل سطح‌متد حذف شد؛ تنها حالت مشترکِ حساس (حافظهٔ نهان) با
`synchronized(cache)` محافظت می‌شود. `clearCache` هم روی قفل درست (`cache`) قرار
گرفت (قبلاً روی قفل نادرستِ نمونه قفل می‌شد و ریسک CME داشت).

### ۷. نشتی fd و نخ با شروع دوبارهٔ سرویس
هر فراخوانی دوبارهٔ `onStartCommand` یک `establish()` و نخ تونل جدید می‌ساخت و
fd قبلی نشت می‌کرد؛ استخر نخ‌های shutdown شده هم `RejectedExecutionException` می‌داد.
**اصلاح:** نگه‌داری `thread` با قفل + مسیر «از قبل روشن است → فقط applySettings»،
بازسازی استخر در هر دور، و `runCatching` دور `pool.execute`. حلقهٔ تونل هم با
«هویت نخ» (`thread === me`) کنترل می‌شود تا نخ کهنه بعد از ری‌استارت زامبی نشود.

### ۸. اعلان پایدار در اندروید ۱۳+ دیده نمی‌شد
مجوز `POST_NOTIFICATIONS` هرگز درخواست نمی‌شد → اعلان (و دکمهٔ قطع روی آن) مخفی می‌ماند.
**اصلاح:** مجوز به مانیفست اضافه و در اولین اجرای برنامه درخواست می‌شود.

### ۹. تنظیمات UI وب‌ویو کاری انجام نمی‌داد (نسخهٔ ۲.۰)
سوییچ‌های تنظیمات (مسدودسازی تبلیغات، Sinkhole، بررسی گواهی، بازگشت به DNS ساده،
شروع خودکار، IPv6) فقط در حافظهٔ JS تغییر می‌کردند — نه ذخیره می‌شدند نه روی تونل
اعمال می‌شدند. انتخاب پروتکل هر سرور هم همین‌طور بود.
**اصلاح:** پل کامل `getSettings` / `setSetting` / `setProto` / `setTheme` ساخته شد؛
هر تغییر بی‌درنگ ذخیره و با `applySettings()` روی تونلِ روشن اعمال می‌شود و در
اجرای بعدی برنامه از نو خوانده می‌شود.

### ۱۰. بنچمارک کاملاً جعلی بود (نسخهٔ ۲.۰)
هر سه دکمهٔ سنجش، عدد تصادفی `18+random()*330` را «تأخیر» جلوه می‌دادند و
«انتخاب بهترین DNS» بهترینِ همان اعداد تصادفی بود! کلاس واقعی `Benchmark.kt` اصلاً
به UI وصل نبود.
**اصلاح:** بنچمارک واقعی از طریق پل `bench`/`benchAll` وصل شد؛ سنجش همهٔ سرورها
**موازی** است (الگوی `Race` در Intra) با مهلت ۳ ثانیه برای هر سرورِ مرده؛ نتیجهٔ هر
سرور به‌محض آماده‌شدن به UI هل داده می‌شود. حالت شبیه‌سازی فقط برای پیش‌نمایش مرورگر
مانده است.

### ۱۱. سرورهای سفارشی بعد از بستن برنامه گم می‌شدند (نسخهٔ ۲.۰)
در کاتلین ذخیره می‌شدند ولی UI دوباره نمی‌خواندشان؛ شناسهٔ JS با شناسهٔ کاتلین هم
فرق داشت.
**اصلاح:** `customServers()` برای بازیابی در شروع + `addServer` شناسهٔ واقعی
ذخیره‌شده را برمی‌گرداند.

### ۱۲. کاشی تنظیمات سریع «قصد اتصال» را نادیده می‌گرفت
`TunnelTile` با `EXTRA_CONNECT` برنامه را باز می‌کرد ولی `MainActivity` آن را
نمی‌خواند.
**اصلاح:** خواندن extra در `onCreate`/`onNewIntent` و اتصال خودکار (با احترام به
گفت‌وگوی مجوز VPN).

---

## 🟡 بهبودهای سازگاری و پایداری (الگوی Intra/DNS66)

- **`setMetered(false)`** در اندروید ۱۰+ — وگرنه برنامه‌ها VPN را شبکهٔ «متریک»
  می‌بینند و دادهٔ پس‌زمینه را محدود می‌کنند (Intra و DNS66 هر دو این کار را می‌کنند).
- **`allowFamily(AF_INET/AF_INET6)`** — تا ترافیک IPv6 شبکهٔ اصلی وقتی تونل فقط
  IPv4 دارد مسدود نشود (تجربهٔ DNS66، issue 129).
- **`establish()` روی نخ پس‌زمینه** — در Intra ساخت تونل هرگز روی نخ اصلی نیست.
- **سرویس VPN `exported=false`** شد (مانند Intra) — قبلاً `exported=true` بود و هر
  برنامهٔ دیگری می‌توانست با `stopService` تونل را بکُشد.
- **BootReceiver اول `VpnService.prepare` را چک می‌کند** (الگوی `AutoStarter` در
  Intra) — اگر مجوز VPN نباشد، سرویس بی‌خود بالا و پایین نمی‌شود.
- **`onRevoke` شروع خودکار را خاموش می‌کند** (الگوی Intra) — اگر کاربر مجوز VPN را
  پس بگیرد، هر بوت تلاش بی‌فایده نمی‌کند.
- **`allowFileAccess=false`** در WebView — برای بارگذاری asset لازم نیست و سطح حمله
  را پایین می‌آورد.
- اگر حلقهٔ تونل به دلیل خطای غیرمنتظره تمام شود، سرویس خودش را خاموش می‌کند تا
  «foregroundِ بی‌تونل» باقی نماند.

---

## 🔧 سایر تغییرات

- `Benchmark`: پارامتر `timeoutMs` برای سنجش سریع؛ حذف `suspend` بی‌استفاده.
- `UpstreamFactory.create(server, prefs, timeoutMs)` برای ساخت ریزالور با مهلت دلخواه.
- `versionCode=2`, `versionName="2.1"`.
- تمام ۳۸ تست قبلی بدون تغییر معتبرند و تست زندهٔ DoT اکنون تأیید واقعی گواهی را
  هم می‌سنجد.

## 🔐 یادآوری امنیتی
فایل `jbdns.keystore` و رمز آن (`keystore.properties`) هنوز در مخزن هستند — قبل از
هر انتشار عمومی حتماً کلید خصوصی جدید بسازید و آن‌ها را از مخزن حذف کنید.
