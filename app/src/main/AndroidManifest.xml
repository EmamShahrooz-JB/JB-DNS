<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        الزامی برای سرویس foreground از نوع connectedDevice در اندروید ۱۴+
        (دقیقاً همان مجوزی که Intra برای همین منظور دارد) — بدون آن
        startForeground(..., CONNECTED_DEVICE) با SecurityException می‌میرد
        و تونل همراه بسته‌شدن برنامه از بین می‌رود.
    -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- الزامی از اندروید ۱۴ به بالا برای سرویس‌های foreground دارای نوع connectedDevice -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- اندروید ۱۳+: بدون آن اعلان پایدار تونل دیده نمی‌شود -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        تونل افتراقی: دیدن فهرست برنامه‌های نصب‌شده در اندروید ۱۱+ بدون نیاز به
        مجوز حساس QUERY_ALL_PACKAGES (الگوی توصیه‌شدهٔ گوگل — Intra آن مجوز را
        دارد، اما مسیر استاندارد <queries> برای فهرست برنامه‌های قابل اجرا کافی است).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".JbDnsApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JBDNS"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.JBDNS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            exported="false": فقط سیستم (که BIND_VPN_SERVICE دارد) می‌تواند به سرویس وصل شود؛
            برنامه‌های دیگر نمی‌توانند آن را خاموش/روشن کنند (الگوی Intra).
            foregroundServiceType از اندروید ۱۴ به بالا الزامی است وگرنه
            MissingForegroundServiceTypeException رخ می‌دهد.
        -->
        <service
            android:name=".net.DnsVpnService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            android:stopWithTask="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <meta-data
                android:name="android.net.VpnService.SUPPORTS_ALWAYS_ON"
                android:value="true" />
        </service>

        <service
            android:name=".core.TunnelTile"
            android:exported="true"
            android:icon="@drawable/ic_stat_dns"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- ویجت صفحهٔ اصلی: دکمهٔ وصل/قطع با یک ضربه -->
        <receiver
            android:name=".ui.TunnelWidget"
            android:exported="false"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/tunnel_widget_info" />
        </receiver>

        <!-- اشتراک فایل پشتیبان تنظیمات (خروجی/ورودی) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".core.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
